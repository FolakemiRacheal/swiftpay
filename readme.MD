configure paystack in the settings by installing python-decouple to be use to access our secret key that will be store in our .env file, so we access it by using from decouple import config


Intialize transaction ensures we have full controll of the transaction details

write paystackcheckout or verificationcode that is where the service.py files enter

then install the requests library

then create checkout functions inside thee service.py for paystack checkout session for users